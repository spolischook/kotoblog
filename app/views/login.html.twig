{% extends 'layout.html.twig' %}
{% block styles %}
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/signin.css" rel="stylesheet">
{% endblock %}
{% block container %}

    {% if error %}
        <div class="alert alert-danger">{{ error }}</div>
    {% endif %}

    <form class="form-signin" role="form" action="{{ path('admin_login_check') }}" method="post">
        <h2 class="form-signin-heading">Please sign in</h2>
        {#<input type="text" name="form[username]" value="{{ last_username }}" class="form-control" placeholder="Username" required autofocus>#}
        {#<input type="password" name="form[password]" class="form-control" placeholder="Password" required>#}
        {#<label class="checkbox">#}
            {#<input type="checkbox" value="remember-me"> Remember me#}
        {#</label>#}
        {#<button class="btn btn-lg btn-primary btn-block" type="submit">Sign in</button>#}
        {{ form_widget(form.username) }}
        {{ form_widget(form.password) }}
        {{ form_widget(form.login) }}
    </form>
{% endblock container %}
